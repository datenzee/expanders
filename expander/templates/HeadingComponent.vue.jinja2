<script setup>
{% if predicate -%}
import * as $rdf from 'rdflib'
{%- endif  %}
import Heading from '@/components/duo/Heading.vue'
// <custom-imports>
// </custom-imports>

const props = defineProps([
    'graph',
    'depth',
    'object',
    // <custom-props>
    // </custom-props>
])

{% if predicate %}
const predicate = $rdf.sym('{{ predicate }}')
{% endif %}

// <custom-code>
// </custom-code>
</script>
<template>
    {% if predicate -%}
    <Heading :depth="depth" :text="{{ 'graph?.match(object, predicate)[0]?.object.value' }}" />
    {%- else -%}
    <Heading :depth="depth" text="{{ text }}" />
    {%- endif %}
</template>
